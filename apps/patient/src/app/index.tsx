import { StyleSheet, Text } from 'react-native';

const Index = () => {
    return (
            <Text style={styles.text}>This is the patient app</Text>
    );
}

const styles = StyleSheet.create({
    text: {
        fontSize: 20,
    },
});

export default Index;
